# CPP-GameDev-Examples
Simple games written in C++ using 'raylib', following examples from GameDev.tv

Note: Laptop/Desktop suffixes exist because I was too lazy to change the git ignore for the 
vscode workspace.
